# ─────────────────────────────────────────────
# 認証情報（実値を含む。絶対にコミットしない）
# 先頭に / を付けず、どの階層にあっても除外する。
# 雛形の .env.example / accounts.example.json は名前が異なるため追跡される。
# ─────────────────────────────────────────────
.env
accounts.json

# ─────────────────────────────────────────────
# 個人の閲覧履歴を含む実行ログ
# ルート直下のみを対象にする（サブディレクトリの logs/ は巻き込まない）
# ─────────────────────────────────────────────
/logs/

# ─────────────────────────────────────────────
# Python の仮想環境（ルート直下のみ）
# ─────────────────────────────────────────────
/.venv/

# ─────────────────────────────────────────────
# Python のキャッシュ
# src/ tests/ tools/ の各階層に生成されるため、あえて全階層を対象にする
# ─────────────────────────────────────────────
__pycache__/
*.py[cod]

# ─────────────────────────────────────────────
# Claude Code のローカル設定
# .claude/ ごとではなくこのファイルのみ除外し、将来の共有設定は追跡できるようにする
# ─────────────────────────────────────────────
/.claude/settings.local.json

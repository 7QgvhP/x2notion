# 変更履歴

本プロジェクトは [セマンティックバージョニング](https://semver.org/lang/ja/) に従う。

> **このリポジトリは v1.8.2 の時点から git 管理を開始した。**
> v1.0.0 から v1.8.2 までは git 管理外で開発していたため、個々のコミット履歴は存在しない。
> 下記の「git管理開始以前の経緯」に、それまでの主な変更点を記録として残す。

## [1.8.2] - 2026-09-20

git 管理の開始時点。以下の機能を備えた状態を初版リリースとする。

### 機能

- Xのブックマークを取得し、Notionのデータベースへ一括登録する
- 複数アカウントに対応し、記載順に連続で取り込む
- ツイートIDを鍵に重複判定するため、何度実行しても新規分だけが追加される
- 取り込み後にXのブックマークから削除できる（`--remove-from-x`、既定は無効）
- Windowsタスクスケジューラへの自動実行登録（`schedule`）
- ダブルクリックで実行できるバッチファイル3種
- Notion側のプロパティ名の変更・削除に追従する対応表（`properties.json`）

### 仕様

- 認証はCookie方式のみ。Xが自動操作でのログインを制限しているため
- タイトルは本文の先頭60文字。超える場合は末尾に `...` を付ける
- 改行は削除して1行に繋げる。ただし前後がどちらも半角英数字の場合のみ空白を残す

---

## git管理開始以前の経緯

記録として残すもので、各バージョンにタグは存在しない。

- **1.0.0** 初版。Xのブックマーク取得とNotionへの登録
- **1.0.1** Notionのページ未共有・Chromium未取得を検知して対処法を案内するよう改善
- **1.0.2** 標準出力と標準エラーの表示順が入れ替わる問題を修正
- **1.1.0** Cookie方式の認証を追加。Xが自動操作でのログインを制限しているため
- **1.2.0** プロパティ構成を実行前に検証し、重複判定が働かない状態での二重登録を防止
- **1.3.0** 複数アカウント対応。`accounts.json` と「アカウント」プロパティを追加
- **1.4.0** 自動実行の登録機能とログ出力を追加
- **1.5.0** プロパティ名の対応表を導入し、Notion側の改名・削除に追従できるようにした
- **1.6.0** 取り込み後にXのブックマークから削除する機能を追加
- **1.7.0** 役目を終えた機能を削除し、ブラウザ起動処理の重複を整理（2,003行 → 1,677行）
- **1.7.1** Xのブックマーク画面のURL変更（`/i/bookmarks` → `/i/history`）で認証失敗と誤検知する不具合を修正
- **1.7.2** 重複判定の鍵をURL文字列からツイートIDへ変更。@ハンドル改名時の二重登録を防止
- **1.8.0** タイトルを「本文1行目」から「先頭60文字」に変更
- **1.8.1** タイトル生成時、改行を空白に置換せず削除するよう変更
- **1.8.2** 改行の前後がどちらも半角英数字の場合のみ空白を残すよう調整
